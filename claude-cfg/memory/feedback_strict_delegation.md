---
name: Strict delegation and tools usage
description: Always use skills, MCP servers, and delegate to subagents — even for simple tasks
type: feedback
---

Always use skills, MCP tools (Serena, Context7, Sequential Thinking), and delegate to subagents — even for seemingly simple tasks.

**Why:** Without these tools I repeatedly make errors, bloat context, and force session restarts. Practice has shown this happens consistently even on "simple" tasks.

**How to apply:** No exceptions. Every code task → check skills first, use MCP tools, delegate to agents. Never try to "just quickly do it myself" — this always leads to worse outcomes.
